<Page
    x:Class="云湖WP.TextViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:云湖WP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Style x:Key="LargeTextViewerTextBoxStyle" TargetType="TextBox">
            <Setter Property="MinWidth" Value="{ThemeResource TextControlThemeMinWidth}" />
            <Setter Property="MinHeight" Value="{ThemeResource TextControlThemeMinHeight}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#18FFFFFF" />
            <Setter Property="BorderBrush" Value="{ThemeResource PhoneAccentBrush}" />
            <Setter Property="SelectionHighlightColor" Value="{ThemeResource PhoneAccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontFamily" Value="{ThemeResource PhoneFontFamilyNormal}" />
            <Setter Property="FontSize" Value="17" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="Padding" Value="12" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <Border x:Name="BackgroundElement"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                                <ScrollViewer x:Name="ContentElement"
                                              AutomationProperties.AccessibilityView="Raw"
                                              HorizontalScrollMode="Disabled"
                                              VerticalScrollMode="Auto"
                                              HorizontalScrollBarVisibility="Disabled"
                                              VerticalScrollBarVisibility="Auto"
                                              IsDeferredScrollingEnabled="False"
                                              Margin="{TemplateBinding BorderThickness}"
                                              Padding="{TemplateBinding Padding}"
                                              Foreground="White" />
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Metro 风格大标题 (云湖 / 查看与复制) -->
        <StackPanel Grid.Row="0" Margin="19,10,19,6">
            <TextBlock Text="云湖" FontSize="11" FontWeight="Normal" Foreground="{ThemeResource PhoneMidBrush}" />
            <TextBlock x:Name="TxtPageTitle" Text="查看与复制" FontSize="42" FontWeight="Light" Margin="0,-4,0,0" />
        </StackPanel>

        <!-- 全屏巨大文本输入/查看框 (高对比度纯白字体，支持任意滚动、长按选中、全选、复制、编辑) -->
        <Grid Grid.Row="1" Margin="16,0,16,16">
            <TextBox x:Name="TxtMainContent"
                     Style="{StaticResource LargeTextViewerTextBoxStyle}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     FontSize="17"
                     Foreground="White"
                     HorizontalAlignment="Stretch"
                     VerticalAlignment="Stretch" />
        </Grid>
    </Grid>

    <Page.BottomAppBar>
        <CommandBar x:Name="ViewerCommandBar" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" ClosedDisplayMode="Compact">
            <CommandBar.PrimaryCommands>
                <AppBarButton x:Name="AppBarBtnSelectAll" Icon="SelectAll" Label="全选" Click="AppBarBtnSelectAll_Click" />
                <AppBarButton x:Name="AppBarBtnClear" Icon="Clear" Label="清空" Click="AppBarBtnClear_Click" />
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Page.BottomAppBar>
</Page>
