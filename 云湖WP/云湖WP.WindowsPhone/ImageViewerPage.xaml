<Page
    x:Class="云湖WP.ImageViewerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:云湖WP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Black">

    <Grid Background="Black">
        <!-- 顶部加载进度条 -->
        <ProgressBar x:Name="ImgProgressBar" IsIndeterminate="True" VerticalAlignment="Top" Visibility="Collapsed" Foreground="{ThemeResource PhoneAccentBrush}" Canvas.ZIndex="30" />

        <!-- 纯全屏手势缩放与查看区 -->
        <ScrollViewer x:Name="ImgScrollViewer" 
                      HorizontalScrollBarVisibility="Auto" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollMode="Auto"
                      VerticalScrollMode="Auto"
                      ZoomMode="Enabled" 
                      MinZoomFactor="1.0" 
                      MaxZoomFactor="4.0"
                      Background="Black"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Stretch">
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image x:Name="ImgFull" 
                       Stretch="Uniform" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" />
            </Grid>
        </ScrollViewer>

        <!-- 加载失败提示 -->
        <StackPanel x:Name="PanelError" Visibility="Collapsed" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="20,0" Canvas.ZIndex="20">
            <SymbolIcon Symbol="Cancel" Foreground="#FFAAAAAA" Margin="0,0,0,8" HorizontalAlignment="Center" />
            <TextBlock Text="图片加载失败" FontSize="16" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,6" />
            <Button Content="点击重试" BorderThickness="1" Click="BtnReload_Click" HorizontalAlignment="Center" />
        </StackPanel>

        <!-- 右下角 Metro 悬浮保存按钮 (纯几何直角半透明) -->
        <Button x:Name="BtnSave" 
                Visibility="Collapsed"
                HorizontalAlignment="Right" 
                VerticalAlignment="Bottom" 
                Margin="0,0,18,24" 
                Padding="16,10" 
                Background="#B0000000" 
                BorderBrush="#55FFFFFF" 
                BorderThickness="1" 
                Click="BtnSave_Click" 
                Canvas.ZIndex="25">
            <StackPanel Orientation="Horizontal">
                <SymbolIcon Symbol="Save" Foreground="White" Margin="0,0,8,0" VerticalAlignment="Center" />
                <TextBlock Text="保存" FontSize="15" FontWeight="SemiBold" Foreground="White" VerticalAlignment="Center" />
            </StackPanel>
        </Button>
    </Grid>
</Page>
